$(function() {	$('#logout').click(function() {				$('#ajax-loading').fadeIn('slow');				$(this).attr('disabled', 'disabled');		var el = this;				showMessage('Déconnexion ...');				$.ajax(NEMESIS.BLOG_ROOT+'logout').done(function(data) {			$('#ajax-loading').fadeOut('slow', function() {				$(el).removeAttr('disabled');				hideMessage();				location.replace(NEMESIS.BLOG_ROOT);			});		}, "json");		return false;	});});