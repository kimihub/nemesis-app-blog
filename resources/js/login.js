$(function() {	$('#connect').click(function() {		$('#ajax-loading').fadeIn('slow');		$(this).attr('disabled', 'disabled');		var el = this;		showMessage('Login...');		$.post(NEMESIS.BLOG_ROOT+'login', {'user': $('#user').val(), 'pwd': $('#pwd').val()}).done(function(data) {			console.log(data);			$('#ajax-loading').fadeOut('slow', function() {				$(el).removeAttr('disabled');				if (data.error)					showMessage('<span class="error">'+data.error+'</span>');				if (data.info)					showMessage(data.info);				if (data.logged)				{					showMessage('Login successful !');					location.replace(NEMESIS.BLOG_ROOT);				}			});		}, "json");		return false;	});});